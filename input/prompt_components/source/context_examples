For this source request, the exact retained context will become the example
text on the card for the contextual sense. For each supplied term:

- Keep the sense used in the retained source context as the first entry for
  that term, and OMIT the "Sentences" property from that entry. The processor
  will insert the exact context that you received; do not repeat, rewrite,
  shorten, or delimit it in "Sentences". In
  "Sentence Translations (English)", provide one complete, natural English
  translation of that exact retained context as a single undelimited text
  block. Translate the whole passage, not only the requested term. If the
  retained context is already Modern English, repeat it as plain text without
  <strong> tags.
- If the term has another genuinely disjoint common lexical sense that meets
  the inclusion criteria, create its separate entry as usual and INCLUDE the
  "Sentences" property with exactly four distinct, pipe-separated example
  sentences. Also include exactly four pipe-separated English translations in
  "Sentence Translations (English)", positionally matched to those examples.
  Each sentence must make that particular additional sense unambiguous.

Only the first, contextual entry for each term may omit "Sentences". Every
additional sense entry must include it. This instruction overrides the general
requirement to generate four example sentences and four translations only for
those first contextual entries; their single translation must instead match
the exact source context that the processor will insert.

For every additional sense, provide exactly three short, natural
{source_language} examples as three plain strings in "Sentences".

Each example must use the complete lexical item belonging to that result's
rank naturally. Use the exact supplied spelling when the language is
uninflected or grammar permits it; otherwise use an ordinary inflected form of
the same lexical item. Do not substitute only a component, substring, or
different derivative. Use no HTML or other formatting and never put | in an
item.

Make each example a complete, idiomatic usage whose grammar unambiguously
demonstrates only that sense. Use a distinct set for each separate sense.

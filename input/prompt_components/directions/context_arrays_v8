Provide exactly three short, natural {source_language} example sentences for
each additional-sense entry. Return them as exactly three separate plain
strings in the "Sentences" JSON array. Never join sentences with | and never
put | inside an array item.

Each sentence must use the complete requested term naturally and demonstrate
only that additional entry's selected sense and grammatical role. Use the
exact supplied spelling when the language is uninflected or grammar permits
it; otherwise use an ordinary inflected form of the same lexical item. Do not
substitute only a component, substring, or different derivative. Use no HTML
or other formatting. Generate a distinct set for every separate sense.
